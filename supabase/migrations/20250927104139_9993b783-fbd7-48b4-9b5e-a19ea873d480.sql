-- CONFIGURA EMAIL HOOK per AUTH PERSONALIZZATA
-- Aggiorna il config per usare la funzione di auth personalizzata

-- Aggiorna la configurazione per usare la nostra funzione di email personalizzata
-- Nota: Questa deve essere configurata anche nel dashboard Supabase

-- Test per verificare che il trigger funzioni
SELECT 'Email trigger configuration ready' as status;